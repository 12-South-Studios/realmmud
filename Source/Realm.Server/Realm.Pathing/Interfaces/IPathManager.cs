namespace Realm.Pathing.Interfaces
{
    public interface IPathManager
    {
        int MaxMobileMovementCost { get; }

        //void RegisterGraph(Zone zone);
        //IList<string> GetShortestPath(Space source, Space destination);
        //Graph BuildGraph(Zone zone);
    }
}